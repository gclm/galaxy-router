type: feature
complexity: moderate
sop: sop/feature.md
status: completed
feature: 2026-07-02-客户端配置生成器
title: 客户端配置生成器 — 纯前端生成 Claude Code / Codex 客户端配置
created: '2026-07-02'
notes: >
  范围（2026-07-02 定稿）：
  纯前端生成两类客户端配置，无后端接口。
  ① Claude Code → ~/.claude/settings.json：三档位 env（ANTHROPIC_DEFAULT_{SONNET,OPUS,HAIKU}_MODEL）
  + 三开关（attribution 顶层 key / effortLevel 顶层 key / DISABLE_AUTOUPDATER env），
  字段映射已抓官方 code.claude.com/docs/en/settings 核实；
  ② Codex → ~/.codex/config.toml（官方 env_key 方式，model_providers.galaxy + wire_api=chat）
  + ~/.codex/.env（放 GALAXY_API_KEY），key 不入 auth.json。
  UI：单栏流式 + 实时只读预览 + 折叠原始 JSON + 安装引导手风琴。
  触发：创建 key 成功弹入口（key 预填）+ 独立页 /client-config（侧边栏外链改内部路由）。
  Gate 1 design status=approved（2026-07-02 用户确认）。effortLevel 默认 high；wire_api=responses（用户确认最新 Codex 走 Responses API）。Gate 2 开工。
  A-1 完成（env-vars 页核实）：三档位变量名 ANTHROPIC_DEFAULT_{SONNET,OPUS,HAIKU}_MODEL 确认；effortMax 改用 env CLAUDE_CODE_EFFORT_LEVEL:"max"（顶层 effortLevel key 无 max）+ CLAUDE_CODE_ALWAYS_ENABLE_EFFORT:"1"（galaxy 用自定义 model ID，不加则 effort 静默失效）。design §2.4/§3 已同步。
  Gate 3 验收通过（联调 S1-S9 + C1-C5 + D1/D2 全绿）；commit a9944f7 push main。
