design: 2026-07-02-客户端配置生成器
status: in_progress
steps:
  # Phase A：纯函数
  - { id: A-1, desc: "fetch code.claude.com/docs/en/env-vars 核实三档位 env 变量名（附带发现 effortMax 需配 ALWAYS_ENABLE_EFFORT）", status: done }
  - { id: A-2, desc: "clientConfig.ts：generateClaudeConfig + generateCodexConfig（wire_api=responses）", status: done }
  - { id: A-3, desc: "§3 契约（C1-C5/D1-D2）人工核对；项目无测试框架不写单测", status: done }
  # Phase B：UI 页
  - { id: B-1, desc: "ClientConfig.tsx：Tab + key 下拉 + base_url + 三档位/Codex 单模型 + 三开关 + 实时预览 + 复制/下载 + 安装手风琴", status: done }
  - { id: B-2, desc: "App.tsx 注册 <Route path=client-config> + pages/index.ts re-export", status: done }
  # Phase C：接入触发点
  - { id: C-1, desc: "ApiKeyForm 成功视图加「生成客户端配置」按钮 → onGenerateConfig；ApiKeys useNavigate 传 {state:{apiKey}}", status: done }
  - { id: C-2, desc: "ClientConfig.tsx 读 useLocation().state?.apiKey 预填 key", status: done }
  - { id: C-3, desc: "Sidebar.tsx 外链 <a DOCS_URL> → <Link to=/client-config>（带激活态）+ 删孤儿 DOCS_URL", status: done }
  # Phase D：验收
  - { id: D-1, desc: "tsc -b + pnpm build 全绿；本次改动 0 新增 lint error（项目 pre-existing 21 个 set-state-in-effect error 非本次引入，App.tsx:35 NavigationProgress 同）", status: done }
  - { id: D-2, desc: "chrome-devtools 联调：S1-S9 + C1-C5 + D1/D2 全通过（含创建 key→入口→预填、Codex responses、复制 toast）", status: done }
  - { id: D-3, desc: "写 report.md（验收契约核对 + 测试结果 + 挂载点检查）", status: done }
