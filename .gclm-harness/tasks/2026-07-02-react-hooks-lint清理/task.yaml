type: refactor
complexity: moderate
sop: sop/refactor.md
status: completed
feature: 2026-07-02-react-hooks-lint清理
title: 前端 react-hooks lint 清零（refs/set-state-in-effect/exhaustive-deps 共 28）
created: '2026-07-02'
notes: >
  清零全项目 28 个 react-hooks lint problems（19 errors + 9 warnings）：
  - refs 13：Playground.tsx 149-159（saved.useRef 在 render 读 → useState lazy init，12）+ App.tsx:44（1）
  - set-state-in-effect 6：App.tsx:38/75/107/108 + Playground.tsx:202 + Settings.tsx:541
  - exhaustive-deps 9：ChannelStats:18(3) + ModelStats:16(2) + ApiKeyStats:14(2) + Playground:201(1) + Dashboard:87(1)
  修法：refs → useState lazy init（不在 render 读 ref.current）；
  set-state-in-effect → derived state 优先，响应外部变化的副作用场景按需 disable + 注释；
  exhaustive-deps → 补全依赖或 disable。
  不写独立 design（lint 修法由规则固定，无需设计决策）。Gate 2 逐文件实现 + Gate 3 lint 验证。
