design: 2026-07-01-智能调度打分重构
status: completed
steps:
  # P1 打分层 — done
  - { id: P1-1, desc: "capacity.rs 加 pub fn active_count", status: done }
  - { id: P1-2, desc: "scoring.rs SchedulerScoreWeights 重构（latency/least_conn/error/health）", status: done }
  - { id: P1-3, desc: "scoring.rs CandidateScoreInput 重构（active_concurrency/min/max_active）", status: done }
  - { id: P1-4, desc: "scoring.rs calculate_candidate_score 新公式 + 冷启动0.8", status: done }
  - { id: P1-5, desc: "scoring.rs compare 删 load/waiting/priority", status: done }
  - { id: P1-6, desc: "candidates.rs score_candidates 两遍扫", status: done }
  - { id: P1-7, desc: "scoring.rs 内联测试 5 个", status: done }
  - { id: P1-8, desc: "cargo test scoring(5)+candidates 通过", status: done }
  # P2 候选层 — done
  - { id: P2-1, desc: "candidates.rs build_relay_candidates priority 分档 + tier_and_demote", status: done }
  - { id: P2-2, desc: "tier_and_demote 单测 3 个", status: done }
  - { id: P2-3, desc: "cargo check + 全量 lib test 通过（260）", status: done }
  # P3 前端 — done
  - { id: P3-1, desc: "GroupForm 隐藏重试 + priority 文案 / Groups 重试列 / ChannelForm max_concurrency 标注", status: done }
  - { id: P3-2, desc: "pnpm build 通过", status: done }
  # P4 DB 清理 — done
  - { id: P4-1, desc: "迁移 15 DROP max_retries/custom_headers/extras + 全链路 admin/backup/前端 移除", status: done }
  - { id: P4-2, desc: "全量 cargo test 通过（lib+集成 0 failed）+ 重写 scheduler_scoring_tdd + 修复 integration_test fixture", status: done }
